const { volidateBody } = require("./validateBody");
const { isValidId } = require("./isValidId");
const { isFavoriteField } = require("./isFavoriteField");

module.exports = {
  volidateBody,
  isValidId,
  isFavoriteField,
};